<nav
  class="
    navbar navbar-expand navbar-light
    bg-light
    my-5
    rounded
    w-100-sm w-75
    py-2
    m-auto
    sticky-top
  "
>
  <div class="container-fluid">
    <!-- title -->
    <a class="navbar-brand" href="/">Lords of the Memes</a>

    <!-- menu -->
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav align-items-center">
        <% if (!user) { %>
        <li class="nav-item">
          <a
            class="btn btn-dark text-light spacing rounded-pill fw-bold mx-2"
            aria-current="page"
            href="/signup"
            >Sign Up</a
          >
        </li>

        <li class="nav-item">
          <a
            class="btn btn-dark text-light spacing rounded-pill fw-bold mx-2"
            href="/login"
            >Login</a
          >
        </li>

        <% } else { %>

        <li class="nav-item">
          <a
            class="btn btn-dark text-light spacing rounded-pill fw-bold"
            href="/new-message"
            >Create Message</a
          >
        </li>

        <li class="nav-item dropdown px-3">
          <a
            class="nav-link dropdown-toggle"
            href="#"
            id="navbarDropdownMenuLink"
            role="button"
            data-bs-toggle="dropdown"
            aria-expanded="false"
          >
            <% if(!user.avatar) { %>
            <img
              src="/images/avatars/default.png"
              alt="Avatar"
              class="rounded-circle profile border"
            />

            <% } else { %>
            <!-- prettier-ignore -->
            <% let avatar = `/images/avatars/${user.avatar}.jpg` %>
            <img
              src="<%=avatar%>"
              alt="Avatar"
              class="rounded-circle profile border"
            />
            <% } %>
          </a>

          <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <li>
              <a class="dropdown-item" href="/profile/<%=user._id%>"
                >View Profile</a
              >
            </li>

            <% if (!user.admin) { %>
            <li>
              <a class="dropdown-item" href="/admin-form">Become an admin</a>
            </li>

            <% } %>
            <li>
              <a class="dropdown-item" href="/logout">Log Out</a>
            </li>
          </ul>
        </li>

        <% } %>
      </ul>
    </div>
  </div>
</nav>
